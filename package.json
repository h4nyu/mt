{
  "name": "kaguya",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "yarn workspaces foreach -pv --from '@kaguya/*' run clean",
    "build": "yarn workspaces foreach -pvR --topological-dev --from '@kaguya/*' run build"
  },
  "workspaces": [
    "core",
    "web",
    "server",
    "api",
    "cli"
  ],
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "packageManager": "yarn@3.2.1"
}
